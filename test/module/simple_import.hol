% Sections 1.3 / 2.2: `import test.module.util.` brings every declaration
% of the helper module into the main file's scope, so `triple` is in
% scope below.
import test.module.util.

type drop_one (nat -> o).
drop_one X :- triple X, X > 3.
